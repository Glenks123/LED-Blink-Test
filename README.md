A quick hello-world bare-metal firmware application that controls a digital LED via button input, utilizing female-to-female jumper wires that turns the LED at pin PD12 on when connected to VDD and vice versa when connected to GND. Bypassing the HAL, the code directly manipulates memory-mapped registers to enable peripheral clocks, configure GPIO modes, and run an infinite polling loop to read and drive pin states
